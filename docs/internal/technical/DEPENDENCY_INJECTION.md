# Dependency Injection

Use `get_it` for dependency registration. Register abstractions near composition roots and avoid service locator calls deep inside domain entities.

## Required checks

- [ ] Relevant requirement or decision is linked.
- [ ] Implementation remains offline-first.
- [ ] Verification evidence is recorded when status changes.
